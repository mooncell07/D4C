[tool.poetry]
name = "D4C"
version = "0.1.0"
description = ""
authors = ["Emiya97 <importz750@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
pytest = "^7.1.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
